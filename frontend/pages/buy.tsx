import { Fragment } from "react";
import { NextPage } from "next";
import { default as NextHead } from "next/head";

const Buy: NextPage = () => {
  return (
    <Fragment>
      <NextHead>
        <title>Buy Ticket | BlocEvent - A decentralized events app</title>
      </NextHead>
    </Fragment>
  );
};
export default Buy;
